# Data Source Configurations
spring.datasource.test-while-idle=true
spring.datasource.test-on-borrow=true
spring.datasource.test-on-return=false
spring.datasource.validation-query=SELECT 1
spring.datasource.validation-interval=30000
spring.datasource.time-between-eviction-runs-millis=30000
spring.datasource.max-active=${db.pool.max.active}
spring.datasource.min-idle=${db.pool.min.idle}
spring.datasource.max-wait=${db.pool.max.wait}
spring.datasource.initial-size=${db.pool.initial.size}
spring.datasource.remove-abandoned-timeout=60
spring.datasource.remove-abandoned=true
spring.datasource.log-abandoned=true
spring.datasource.min-evictable-idle-time-millis=30000
spring.datasource.jmx-enabled=true
spring.datasource.jdbc-interceptors=org.apache.tomcat.jdbc.pool.interceptor.ConnectionState;org.apache.tomcat.jdbc.pool.interceptor.StatementFinalizer
spring.datasource.username=${db.username}
spring.datasource.password=${db.password}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${db.hostname}:${db.port}/${db.database}

# JPA Configurations
spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.DefaultNamingStrategy

# JSP/JSTL Configurations
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp
